---
name: minimax-multimodal-toolkit
description: ⚠️ DEPRECATED — This skill is a duplicate of mmx-cli. Use mmx-cli instead for all MiniMax CLI operations. This file kept for backward compatibility only.
tags: [minimax, deprecated, cli, media]
status: deprecated
replaced-by: mmx-cli
---

> ⚠️ **DEPRECATED**: This skill is a duplicate of `mmx-cli`. All functionality has been consolidated into `mmx-cli/SKILL.md`. This file is retained only for backward compatibility — new implementations should use the `mmx-cli` skill.
>
> See [mmx-cli](../mmx-cli/SKILL.md) for the current version.

## When to Use
- ⛔ **DO NOT USE THIS SKILL.** Use `mmx-cli` instead.
- Existing projects that still reference `minimax-multimodal-toolkit` by name

## Do Not Use
- All new development — use `mmx-cli` for MiniMax CLI operations

# MiniMax CLI — Agent Skill Guide (DEPRECATED)

**This skill is deprecated. Use `mmx-cli` instead.**

The complete, current command reference is at `../mmx-cli/SKILL.md` with full details in `../mmx-cli/references/commands.md`.

## Quick Migration
```bash
# Old references → new skill
minimax-multimodal-toolkit    →   mmx-cli
```

Same commands, same syntax, same flags — just a renamed and better-organized skill.
